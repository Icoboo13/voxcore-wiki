# ⚙️ Configuration

VoxCore is fully configurable via `config.yml`.

---

## Example

```yaml
chat:
  format: "&7[%rank%] %player%: %message%"
  enable-colors: true

channels:
  global:
    enabled: true
  staff:
    permission: "voxcore.staff"
